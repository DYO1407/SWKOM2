using Xunit;
using DataAccess.Entities; // Make sure this using directive is present

namespace DataAccess.Tests.Entities
{
    /*public class DocumentTagTests
    {
        [Fact]
        public void DocumentTagClassPropertiesShouldHaveCorrectDataTypes()
        {
            // Arrange
            var documentTag = new DocumentTag();

            // Act and Assert
            Assert.IsType<int>(documentTag.Id);
            Assert.IsType<string>(documentTag.Slug);
            Assert.IsType<string>(documentTag.Name);
            Assert.IsType<string>(documentTag.Color);
            Assert.IsType<string>(documentTag.Match);
            Assert.IsType<int>(documentTag.MatchingAlgorithm);
            Assert.IsType<bool>(documentTag.IsInsensitive);
            Assert.IsType<bool>(documentTag.IsInboxTag);
            Assert.IsType<int>(documentTag.DocumentCount);
        }

        [Fact]
        public void DocumentTagPropertiesShouldAllowNullOrEmptyValues()
        {
            // Arrange
            var documentTag = new DocumentTag();

            // Act and Assert
            Assert.Null(documentTag.Slug);
            Assert.Null(documentTag.Name);
            Assert.Null(documentTag.Color);
            Assert.Null(documentTag.Match);
        }

        // Add more tests as needed based on specific requirements or validation rules.
    }*/
}
